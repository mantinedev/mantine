import { renderDemo } from '../../../render-demo';
import * as demos from './index';

export default { title: 'usePagination' };

export const Demo_startValue = {
  name: '⭐ Demo: startValue',
  render: renderDemo(demos.startValue),
};
