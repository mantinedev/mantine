import { renderDemo } from '../../../render-demo';
import * as demos from './index';

export default { title: 'useNetwork' };

export const Demo_usage = {
  name: '⭐ Demo: usage',
  render: renderDemo(demos.usage),
};
