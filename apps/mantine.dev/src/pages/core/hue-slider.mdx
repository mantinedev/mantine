import { ColorPickerDemos } from '@docs/demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.HueSlider);

## Usage

Use `HueSlider` component to allow users to select hue value of a color.
It accepts values from `0` to `360`. `HueSlider` is a part of [ColorPicker](/core/color-picker) component,
but can also be used separately.

<Demo data={ColorPickerDemos.hueSlider} />
