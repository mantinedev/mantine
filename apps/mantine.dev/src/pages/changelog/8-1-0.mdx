import { HooksDemos } from '@docs/demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.Changelog810);

## Other changes

- Handlers returned from [use-counter](/hooks/use-couter) are now memoized
- Return value of [use-event-listener](/hooks/use-event-listener) changed to `React.RefCallback` (related to [#7406](https://github.com/mantinedev/mantine/issues/7406))
