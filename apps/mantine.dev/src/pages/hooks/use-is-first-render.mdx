import { UseIsFirstRenderDemos } from '@docs/demos';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.useIsFirstRender);

## Usage

The `useIsFirstRender` hook returns `true` if the component is being rendered for the first time;
otherwise, it returns `false`.

<Demo data={UseIsFirstRenderDemos.usage} />

## Definition

```tsx
function useIsFirstRender(): boolean;
```
