import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.GettingStartedSchedule);

# Mantine schedule

[![npm](https://img.shields.io/npm/dm/@mantine/schedule)](https://www.npmjs.com/package/@mantine/schedule)

The [@mantine/schedule](https://www.npmjs.com/package/@mantine/schedule) package
provides more than 70 hooks to build custom components. The `@mantine/schedule`
package is used internally in most other `@mantine/*` packages –
it is required to be installed in your project to use Mantine components.

## Installation

<InstallScript packages="@mantine/schedule dayjs" />

## License

MIT
