import { PaginationStylesNames } from './Pagination';

export const Pagination: Record<PaginationStylesNames, string> = {
  root: 'Root element',
  item: 'Pagination item, applies on page items and arrows',
  dots: 'Styles applied to dots item between page numbers',
  disabled: 'Styles on arrow item while its disabled',
  active: 'Styles on page item while its selected',
};
