import React from 'react';
import { ScrollArea } from '../ScrollArea';

const code = `
`;

function Demo() {
  return <ScrollArea>Demo</ScrollArea>;
}

export const usage: MantineDemo = {
  type: 'demo',
  component: Demo,
  code,
};
