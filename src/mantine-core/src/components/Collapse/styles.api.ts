import { CollapseStylesNames } from './Collapse';

export const Collapse: Record<CollapseStylesNames, string> = {
  root: 'Content wrapper, used to animate height',
  content: 'Inner content, used to animate opacity',
};
