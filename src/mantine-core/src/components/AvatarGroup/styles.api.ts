import { AvatarsGroupStylesNames } from './AvatarsGroup';

export const Avatar: Record<AvatarsGroupStylesNames, string> = {
  root: 'Root element',
  child: 'Styles added to every child inside AvatarsGroup',
  number: 'Truncated avatars count',
};
