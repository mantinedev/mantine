import { MenuLabel } from './MenuLabel';

describe('@mantine/core/MenuLabel', () => {
  it('has correct displayName', () => {
    expect(MenuLabel.displayName).toEqual('@mantine/core/MenuLabel');
  });
});
