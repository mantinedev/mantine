import { ColorSliderStylesNames } from './ColorSlider';

export const ColorSlider: Record<ColorSliderStylesNames, string> = {
  slider: 'Slider root element',
  sliderOverlay: 'Slider overlay, includes gradients and box-shadows',
};
