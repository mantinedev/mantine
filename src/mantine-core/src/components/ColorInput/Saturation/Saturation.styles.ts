import { createMemoStyles } from '../../../theme';

export default createMemoStyles({
  saturationWrapper: {
    position: 'relative',
    height: 200,
  },

  saturationOverlay: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});
