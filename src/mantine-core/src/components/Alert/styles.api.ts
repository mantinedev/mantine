import useStyles from './Alert.styles';

export const Alert: MantineClasses<typeof useStyles> = {
  root: 'Root element',
  title: 'Title element, defined by title prop',
  body: 'Body element, defined by children prop',
};
