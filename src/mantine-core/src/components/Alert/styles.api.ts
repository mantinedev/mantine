import { AlertStylesName } from './Alert';

export const Alert: Record<AlertStylesName, string> = {
  root: 'Root element',
  title: 'Title element',
  icon: 'Icon wrapper',
  body: 'Body element, defined by children prop',
};
