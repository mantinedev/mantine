import { createUseStyles } from 'react-jss';

export default createUseStyles({
  wrapper: {
    position: 'relative',
  },

  input: {
    paddingRight: 35,
  },

  control: {
    position: 'absolute',
    top: 5,
    right: 5,
  },
});
