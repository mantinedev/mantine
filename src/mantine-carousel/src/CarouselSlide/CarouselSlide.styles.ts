import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  slide: {
    position: 'relative',
    flex: '0 0 100%',
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
    height: 200,
  },
}));
