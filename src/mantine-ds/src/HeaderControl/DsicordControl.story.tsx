import React from 'react';
import { DiscordControl } from './DiscordControl';

export default { title: 'DS/DiscordControl' };

export function Usage() {
  return (
    <div style={{ padding: 40 }}>
      <DiscordControl />
    </div>
  );
}
