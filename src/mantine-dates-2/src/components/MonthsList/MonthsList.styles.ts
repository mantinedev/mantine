import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  monthsList: {
    width: '100%',
    borderCollapse: 'collapse',
    borderWidth: 0,
  },

  monthsListCell: {
    padding: 0.5,
  },

  monthsListRow: {},
}));
