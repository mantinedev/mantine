import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  yearsList: {
    width: '100%',
    borderCollapse: 'collapse',
    borderWidth: 0,
  },

  yearsListCell: {
    padding: 0.5,
  },

  yearsListRow: {},
}));
