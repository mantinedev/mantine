import { createUseStyles } from 'react-jss';

export default createUseStyles({
  container: {
    maxWidth: 1000,
    boxSizing: 'border-box',
    paddingLeft: 15,
    paddingRight: 15,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});
