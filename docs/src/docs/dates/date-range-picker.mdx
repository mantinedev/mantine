---
group: 'mantine-dates'
package: '@mantine/dates'
title: DateRangePicker
order: 1
slug: /dates/date-range-picker/
description: 'Capture dates range from user'
props: ['DateRangePicker']
import: "import { DateRangePicker } from '@mantine/dates';"
source: 'mantine-dates/src/components/DateRangePicker/DateRangePicker.tsx'
docs: 'dates/DateRangePicker.mdx'
styles: ['DateRangePicker']
---

import { DateRangePickerDemos } from '@demos/dates';

## Usage

<Demo data={DateRangePickerDemos.usage} />

## Multiple months

You can show two months next to each other by setting `amountOfMonths`:

<Demo data={DateRangePickerDemos.multiMonth} />


## DatePicker props

DateRangePicker component uses the same props as [DatePicker](/dates/date-picker/) component.
The only difference is in `value`, `onChange` and `withMultipleMonths` props shown in previous examples.

## Server side rendering

Component uses [use-id](/hooks/use-id/) hook to generate unique ids and aria- attributes,
provide static `id` prop to prevent props mismatch:

```tsx
<DateRangePicker /> // -> random id generated both on client and server, props mismatch warning
<DateRangePicker id="my-date-range" /> // -> id is static, no mismatches
```

## Accessibility and usability

To make all component controls visible to screen reader set following props:

```tsx
// When withSelect prop set to false
<DateRangePicker
  withSelect={false}
  nextMonthLabel="Next month" // -> aria-label for button that switches to next month
  previousMonthLabel="Previous month" // -> aria-label for button that switches to previous month
/>

// When withSelect prop set to true
<DateRangePicker
  withSelect
  nextMonthLabel="Next month" // -> aria-label for button that switches to next month
  previousMonthLabel="Previous month" // -> aria-label for button that switches to previous month
  yearLabel="Select year" // -> year select aria-label
  monthLabel="Select month" // -> month select aria-label
/>
```
