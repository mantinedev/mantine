---
package: mantine-core
title: Input
order: 1
slug: /core/input/
category: 'inputs'
description: 'Base component to create custom inputs'
props: 'Input'
import: "import { Input } from '@mantine/core';"
---

import { ActionIconUsageDemo } from '../demos/core/ActionIcon/action-icon.demos';
import { InputVariantsDemo } from '../demos/core/Input/input-variants.demo';
import { InputIconDemo } from '../demos/core/Input/input-icon.demo';
import { InputConfigurator } from '../demos/core/Input/input.configurator';
import { InputComponentDemo } from '../demos/core/Input/input-component.demo';
import { InputTooltipDemo } from '../demos/core/Input/input-tooltip.demo';
import { KbdInputDemo } from '../demos/core/Kbd/kbd-input.demo';

# Input

<PageHeader
  source="mantine-core/src/Input/Input.tsx"
  docs="core/Input.mdx"
  package="@mantine/core"
/>

```tsx
import { Input } from '@mantine/core';
```

## Usage

Input component is used as base for all other inputs (<GatsbyLink to="/core/select/">Select</GatsbyLink>, <GatsbyLink to="/core/text-input/">TextInput</GatsbyLink>, <GatsbyLink to="/core/textarea/">Textarea</GatsbyLink> and others).
The single purpose of Input is to provide shared styles and features to other inputs.
Use other components listed above to build forms (as they provide better accessibility)
and Input component as base for your own custom inputs with Mantine theme.
See <GatsbyLink to="/guides/inputs/">guide on inputs</GatsbyLink> to learn how to build custom inputs with this component.

<InputConfigurator />

## Variants

Input has 3 variants, all of which are available on all Mantine inputs.
Note that unstyled input variant may significantly impact usability, use it wisely.

<InputVariantsDemo />

## Icon and right section

You can add icon on the left side of the input, for example:

- [radix icons](https://icons.modulz.app/) (used in mantine packages)
- [feather](https://feathericons.com/)
- [react-icons](https://react-icons.github.io/react-icons)

<InputIconDemo />

Right section allows you to place anything on the right side of the input. For example, in <GatsbyLink to="/core/password-input/">PasswordInput</GatsbyLink>
component right section is used for show/hide password toggle:

<ActionIconUsageDemo />

Or you can add keyboard shortcut like in search on Mantine docs website:

<KbdInputDemo />

Right section with <GatsbyLink to="/core/tooltip/">Tooltip</GatsbyLink>:

<InputTooltipDemo />

## Custom component

As Input component is intended to be a base for all other inputs,
you can pass `component` prop which will define root element:

<InputComponentDemo />

## Get element ref

You can get input ref by passing `elementRef` prop to Input component:

```tsx
import React, { useRef } from 'react';
import { Input } from '@mantine/core';

function Demo() {
  const ref = useRef(null);
  return <Input elementRef={ref} />;
}
```

## TypeScript

You need to pass additional types to use custom component with TypeScript
in case you need full type coverage for events and `elementRef`:

```tsx
<Input<'button', HTMLButtonElement>
  component="button"
  onClick={(event) => console.log(event)}
  elementRef={(node) => {
    window.myRef = node;
  }}
/>

<Input<typeof YourTextarea, HTMLTextareaElement>
  component={YourTextarea}
  onClick={(event) => console.log(event)}
  elementRef={(node) => {
    window.myRef = node;
  }}
/>
```

## Component props

<PropsTable component="Input" />
