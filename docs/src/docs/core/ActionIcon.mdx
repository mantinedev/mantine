---
group: 'mantine-core'
package: '@mantine/core'
title: ActionIcon
order: 1
slug: /core/action-icon/
category: 'inputs'
description: 'Indicate secondary action'
props: ['ActionIcon']
import: "import { ActionIcon } from '@mantine/core';"
source: 'mantine-core/src/components/ActionIcon/ActionIcon.tsx'
docs: 'core/ActionIcon.mdx'
---

import { ActionIconDemos } from '@demos/core';
import { ACTION_ICON_SIZES } from '@mantine/core';

## Usage

You can use any React node as icon by passing it as a child to ActionIcon component.
Note that component **does not control icon size** and you need to specify it manually on icon component for better fit:

```tsx
<ActionIcon>
  <YourIcon style={{ width: 16, height: 16 }} />
</ActionIcon>
```

You can use icons from any react icons library, for example, [radix icons](https://icons.modulz.app/), [feather icons](https://feathericons.com/) or [react-icons](https://react-icons.github.io/react-icons):

<Demo data={ActionIconDemos.configurator} />

## Where to use

Use action icon to indicate secondary action, for example to close modal or toggle password visibility. Keep in mind that component has low emphasis.

Password input visibility toggle example:

<Demo data={ActionIconDemos.usage} />

## Variants

ActionIcon has 5 variants: hover (default), transparent, filled, light and outline:

<Demo data={ActionIconDemos.variants} />

## Color

You can choose any color defined in <GatsbyLink to="/pages/theming/#colors">theme.colors</GatsbyLink>:

```tsx
<ActionIcon color="red" />
<ActionIcon color="blue" />
```

<Demo data={ActionIconDemos.colors} />

## Size

Note that component **does not control icon size** and you need to specify it manually on icon component for better fit.
ActionIcon has predefined sizes: xs, sm, md, lg, xl.
You can pass size as number and it will be set as width and height in px:

```tsx
<ActionIcon size="sm" /> // -> predefined small size
<ActionIcon size={50} /> // -> { width: 50, height: 50 }
```

Sizes from xs to xl:

<Demo data={ActionIconDemos.sizes} />

You can get default sizes values by importing `ACTION_ICON_SIZES`:

```tsx
import { ACTION_ICON_SIZES } from '@mantine/core';
```

<DataTable
  head={['Prop value', 'Width and height']}
  data={Object.keys(ACTION_ICON_SIZES).map((size) => [size, `${ACTION_ICON_SIZES[size]}px`])}
/>

## Radius

Radius has predefined values: xs, sm, md, lg, xl which are defined in <GatsbyLink to="/pages/theming/">theme.radius</GatsbyLink>.
Alternatively you can use number to set border-radius in px:

```tsx
<ActionIcon radius="lg" /> // -> theme predefined large radius
<ActionIcon radius={10} /> // -> { borderRadius: 10 }
<ActionIcon radius={0} /> // -> remove border-radius
```

Default radius from xs to xl:

<Demo data={ActionIconDemos.radius} />

## Get element ref

You can get button ref with `elementRef` prop:

```tsx
import React, { useRef } from 'react';
import { ActionIcon } from '@mantine/core';

function Demo() {
  const ref = useRef();
  return <ActionIcon elementRef={ref} />;
}
```

## Accessibility

ActionIcon renders a regular button element.
Make sure to include `title` or `aria-label` props for screen reader support
as by design element does not have text content.

```tsx
// Title will show message on hover and announce control with screen reader
<ActionIcon title="Settings">
  <GearIcon />
</ActionIcon>

// aria-label will only announce control with screen reader
<ActionIcon aria-label="Settings">
  <GearIcon />
</ActionIcon>
```
