---
group: 'mantine-core'
package: '@mantine/core'
title: ActionIcon
order: 1
slug: /core/action-icon/
category: 'inputs'
description: 'Icon button to indicate secondary action'
props: ['ActionIcon']
import: "import { ActionIcon } from '@mantine/core';"
source: 'mantine-core/src/components/ActionIcon/ActionIcon.tsx'
docs: 'core/ActionIcon.mdx'
styles: ['ActionIcon']
---

import { ActionIconDemos } from '@mantine/demos';

## Usage

<Demo data={ActionIconDemos.configurator} />

## Children

ActionIcon accepts any React node as child. It **does not control icon size**, specify it manually on icon component to match ActionIcon size.
For example, in [tabler icons](https://tablericons.com/) you can use size prop:

```tsx
import { ActionIcon } from '@mantine/core';
import { Adjustments } from 'tabler-icons-react';

function Demo() {
  return (
    <ActionIcon>
      <Adjustments size={16} />
    </ActionIcon>
  );
}
```

## Variants

ActionIcon has 6 variants: hover, default, transparent, filled, light and outline:

<Demo data={ActionIconDemos.variants} />

## Color

ActionIcon supports all colors from [theme.colors](/theming/extend-theme/#default-colors):

```tsx
<ActionIcon color="red" />
<ActionIcon color="blue" />
```

<Demo data={ActionIconDemos.colors} />

## Size and radius

Control button width and height with `size` and border-radius with `radius`.
Both props have predefined values: xs, sm, md, lg, xl.
Alternatively, use a number to set radius or size in px:

```tsx
<ActionIcon radius="lg" /> // -> theme predefined large radius
<ActionIcon radius={10} /> // -> { borderRadius: '10px' }

<ActionIcon size="sm" /> // -> predefined small size
<ActionIcon size={50} /> // -> { width: '50px', height: '50px' }
```

## Close button

CloseButton is a premade ActionIcon with close icon, it is used in all other components: [Popover](/core/popover/), [Modal](/core/modal/), [Notification](/core/Notification/), etc.
Component accepts the same props as ActionIcon with additional `iconSize` prop to control icon width and height:

<Demo data={ActionIconDemos.closeButton} />

## Usage with react-router and other libraries

To use ActionIcon component with [react-router-dom](https://reactrouter.com/web/guides/quick-start)
or other similar libraries pass `Link` as component to ActionIcon:

```tsx
import { Link } from 'react-router-dom';
import { ActionIcon } from '@mantine/core';

function Demo() {
  return (
    <ActionIcon component={Link} to="/react-router">
      React router link
    </ActionIcon>
  );
}
```

To use custom component with TypeScript, provide type (should match `component` prop):

```tsx
import { ActionIcon } from '@mantine/core';
import { Link } from 'react-router-dom';

function Demo() {
  return (
    <>
      <ActionIcon<'a'> component="a" href="#" />
      <ActionIcon<typeof Link> component={Link} to="/hello" />
    </>
  );
}
```

## Get element ref

```tsx
import { useRef } from 'react';
import { ActionIcon } from '@mantine/core';

function Demo() {
  const ref = useRef<HTMLButtonElement>();
  return <ActionIcon ref={ref} />;
}
```

To use `ref` with custom component in TypeScript, specify generic type:

```tsx
import { useRef } from 'react';
import { ActionIcon } from '@mantine/core';

function Demo() {
  const ref = useRef<HTMLAnchorElement>();
  return <ActionIcon<'a'> component="a" href="#" ref={ref} />;
}
```

## Accessibility

ActionIcon renders a regular button element. Set `title` or `aria-label` props for screen reader support:

```tsx
<ActionIcon /> // -> not visible to screen reader
<ActionIcon title="Settings" /> // -> ok
<ActionIcon aria-label="Settings" /> // -> ok
```
