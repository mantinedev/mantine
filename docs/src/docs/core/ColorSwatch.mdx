---
package: mantine-core
title: ColorSwatch
order: 1
slug: /core/color-swatch/
---

import { ColorSwatchBaseDemo } from '../demos/core/ColorSwatch/color-swatch.demos';
import { ColorSwatchComponentDemo } from '../demos/core/ColorSwatch/color-swatch-component.demo';
import { ColorSwatchRadiusDemo } from '../demos/core/ColorSwatch/color-swatch-radius.demo';

# ColorSwatch

<PageHeader
  source="mantine-core/src/ColorSwatch/ColorSwatch.tsx"
  docs="core/ColorSwatch.mdx"
  package="@mantine/core"
/>

```tsx
import { ColorSwatch } from '@mantine/core';
```

## Usage

Use ColorSwatch to display color

```tsx
<ColorSwatch color="#c3c3c3" />
```

Example with default Mantine theme colors:

<ColorSwatchBaseDemo />

## Size

ColorSwatch does not have predefined size values, use number to set width and height in px:

```tsx
<ColorSwatch size={50} /> // -> { width: 50, height: 50 }
```

## Radius

xs, sm, md, lg, xl radius values are defined in <GatsbyLink to="/pages/theming/">theme.radius</GatsbyLink>.
Alternatively you can use number to set radius in px:

```tsx
<ColorSwatch radius="lg" /> // -> theme predefined large radius
<ColorSwatch radius={10} /> // -> { borderRadius: 10 }
```

<ColorSwatchRadiusDemo />

## Custom component

Use any component or element with ColorSwatch by setting `component` prop:

<ColorSwatchComponentDemo />

## Get element ref

You can get root element ref with `elementRef` prop:

```tsx
import React, { useRef } from 'react';
import { Button } from '@mantine/core';

function Demo() {
  const ref = useRef();
  return <ColorSwatch elementRef={ref} />;
}
```

## TypeScript

You need to pass additional types to use custom component with TypeScript
in case you need full type coverage for events and `elementRef`:

```tsx
<ColorSwatch<'button', HTMLButtonElement>
  component="button"
  onClick={(event) => console.log(event)}
  elementRef={(node) => {
    window.myRef = node;
  }}
/>

<ColorSwatch<typeof Link, HTMLAnchorElement>
  component={Link}
  onClick={(event) => console.log(event)}
  elementRef={(node) => {
    window.myRef = node;
  }}
/>
```

## Component props

<PropsTable component="ColorSwatch" />
