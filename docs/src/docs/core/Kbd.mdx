---
group: 'mantine-core'
package: '@mantine/core'
title: Kbd
order: 1
slug: /core/kbd/
category: 'data-display'
description: 'Display keyboard button or keys combination'
props: ['Kbd']
import: "import { Kbd } from '@mantine/core';"
source: 'mantine-core/src/components/Kbd/Kbd.tsx'
docs: 'core/Kbd.mdx'
styles: ['Kbd', 'single-node']
---

import { KbdDemos } from '@demos/core';

## Usage

Use kbd html element to describe keyboard actions and shortcuts:

<Demo data={KbdDemos.usage} />

## Example

Usage example with [TextInput](/core/text-input/) component
– Kbd is used to describe keyboard shortcut to focus input:

<Demo data={KbdDemos.input} />

## TypeScript

Import component props type:

```tsx
import type { KbdProps } from '@mantine/core';
```

`KbdProps` extends `React.ComponentPropsWithoutRef<'kbd'>`, all other props are available under _Component props_ tab.
