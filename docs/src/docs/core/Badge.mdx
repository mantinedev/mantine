---
package: mantine-core
title: Badge
order: 1
slug: /core/badge/
---

import {
  BadgeBaseDemo,
  BadgeLightVariantDemo,
  BadgeOutlineVariantDemo,
  BadgeFilledVariantDemo,
  BadgeComposeDemo,
  BadgeFullWidthDemo,
  BadgeCustomComponentDemo,
} from '../demos/core/Badge/badge.demos';

# Badge

<PageHeader
  source="mantine-core/src/Badge/Badge.tsx"
  docs="core/Badge.mdx"
  package="@mantine/core"
/>

```tsx
import { Badge } from '@mantine/core';
```

## Usage

Badge has 3 variants:

- `light` (default) – light background, dark color, no border
- `outline` – transparent background, dark color and border
- `filled` – dark background, white color, no border

Default Badge color is `theme.primaryColor`. You can choose any color defined in <GatsbyLink to="/theming">theme</GatsbyLink>.

<BadgeBaseDemo />

### Light variant

<BadgeLightVariantDemo />

### Outline variant

<BadgeOutlineVariantDemo />

### Filled variant

<BadgeFilledVariantDemo />

## Full width

Badge can take full width of container if you pass `fullWidth` prop.
If badge is too large for its container, overflow content will be hidden with ellipsis.

<BadgeFullWidthDemo />

## Custom component

You can change badge root element by passing React element to `component` prop.

<BadgeCustomComponentDemo />

## Compose with ElementsGroup

You can compose Badge with itself and any other components that support composition feature with <GatsbyLink to="/core/elements-group">ElementGroup</GatsbyLink> component.

<BadgeComposeDemo />

## Component props

<PropsTable component="Badge" />
