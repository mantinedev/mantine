---
package: mantine-core
title: Hr
order: 1
slug: /core/hr/
category: 'misc'
---

import { HR_SIZES } from '@mantine/core';
import { HrBaseDemo, HrSizesDemo, HrColorDemo } from '../demos/core/Hr/hr.demos';

# Hr

<PageHeader source="mantine-core/src/Hr/Hr.tsx" docs="core/Hr.mdx" package="@mantine/core" />

```tsx
import { Hr } from '@mantine/core';
```

## Usage

Hr component renders horizontal line.
Customize hr border style by setting `variant` prop:

<HrBaseDemo />

## Colors

You can choose any color defined in <GatsbyLink to="/pages/theming/#colors">theme.colors</GatsbyLink>:

<HrColorDemo />

## Sizes

Hr has predefined sizes: xs, sm, md, lg, xl.
You can pass size as number and it will be set as height in px:

<HrSizesDemo />

You can get predefined sizes by importing `HR_SIZES`:

```tsx
import { HR_SIZES } from '@mantine/core';
```

<DataTable
  head={['Prop value', 'Height']}
  data={Object.keys(HR_SIZES).map((size) => [size, `${HR_SIZES[size]}px`])}
/>

## Component props

<PropsTable component="Hr" />
