---
group: 'mantine-core'
package: '@mantine/core'
title: RadioGroup
order: 1
slug: /core/radio-group/
category: 'inputs'
description: 'Capture user feedback limited to small set of options'
props: ['RadioGroup', 'Radio']
import: "import { RadioGroup, Radio } from '@mantine/core';"
source: 'mantine-core/src/components/RadioGroup/RadioGroup.tsx'
docs: 'core/RadioGroup.mdx'
styles: ['RadioGroup']
---

import { RadioGroupDemos } from '@mantine/demos';

## Usage

<Demo data={RadioGroupDemos.configurator} />

## Controlled

```tsx
import { useState } from 'react';
import { RadioGroup, Radio } from '@mantine/core';

function Demo() {
  const [value, setValue] = useState('react');

  return (
    <RadioGroup
      value={value}
      onChange={setValue}
      label="Select your favorite framework/library"
      description="This is anonymous"
      required
    >
      <Radio value="react" label="React" />
      <Radio value="svelte" label="Svelte" />
      <Radio value="ng" label="Angular" />
      <Radio value="vue" label="Vue" />
    </RadioGroup>
  );
}
```

## Size

<Demo data={RadioGroupDemos.sizes} />

## Get input ref

```tsx
import { useRef } from 'react';
import { RadioGroup, Radio } from '@mantine/core';

function Demo() {
  const ref = useRef<HTMLInputElement>();

  return (
    <RadioGroup>
      <Radio value="react" label="React" ref={ref} />
      <Radio value="svelte" label="Svelte" />
      <Radio value="ng" label="Angular" />
      <Radio value="vue" label="Vue" />
    </RadioGroup>
  );
}
```
