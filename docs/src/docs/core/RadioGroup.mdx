---
package: mantine-core
title: RadioGroup
order: 1
slug: /core/radio-group/
category: 'inputs'
---

import { RADIO_SIZES } from '@mantine/core';
import { RadioGroupConfigurator } from '../demos/core/RadioGroup/radio-group.configurator';
import { RadioSizesDemo } from '../demos/core/RadioGroup/radio-sizes.demo';

# RadioGroup

<PageHeader
  source="mantine-core/src/RadioGroup/RadioGroup.tsx"
  docs="core/RadioGroup.mdx"
  package="@mantine/core"
/>

```tsx
import { RadioGroup, Radio } from '@mantine/core';
```

## Usage

Use RadioGroup when you need to capture user feedback limited to certain options.
If you more that 5 options consider using <GatsbyLink to="/core/select">Select</GatsbyLink> instead of RadioGroup,
as it provides better UX for large data sets.

Component supports all props from <GatsbyLink to="/core/input-wrapper">InputWrapper</GatsbyLink> component.
See full information about shared inputs props in <GatsbyLink to="/guides/inputs">inputs guide</GatsbyLink>.

<RadioGroupConfigurator />

## Size

Checkbox has 5 predefined sizes: xs, sm, md, lg, xl.
Size defines label font-size (from <GatsbyLink to="/pages/theming/">theme.fontSizes</GatsbyLink>), input width and height.

<RadioSizesDemo />

You can get checkbox sizes by importing `RADIO_SIZES`:

```tsx
import { RADIO_SIZES } from '@mantine/core';
```

<DataTable
  head={['Prop value', 'Width and height']}
  data={Object.keys(RADIO_SIZES).map((size) => [size, `${RADIO_SIZES[size]}px`])}
/>

## Controlled

```tsx
import React, { useState } from 'react';
import { RadioGroup, Radio } from '@mantine/core';

function Demo() {
  const [value, setValue] = useState('react');

  return (
    <RadioGroup
      value={value}
      onChange={setValue}
      label="Select your favorite framework/library"
      description="This is anonymous, you can safely pick Angular"
      required
    >
      <Radio value="react">React</Radio>
      <Radio value="svelte">Svelte</Radio>
      <Radio value="ng" disabled title="Unless you can't, haha">
        Angular
      </Radio>
      <Radio value="vue">Vue</Radio>
    </RadioGroup>
  );
}
```

## Get element ref

You can get input ref with `elementRef` prop on Radio component:

```tsx
import React, { useRef } from 'react';
import { Select } from '@mantine/core';

function Demo() {
  const ref = useRef();

  return (
    <RadioGroup>
      <Radio value="react">React</Radio>
      <Radio value="svelte" elementRef={ref}>
        Svelte
      </Radio>
    </RadioGroup>
  );
}
```

## RadioGroup component props

<PropsTable component="RadioGroup" />

## Radio component props

<PropsTable component="Radio" />
