---
group: 'mantine-core'
package: '@mantine/core'
title: RadioGroup
order: 1
slug: /core/radio-group/
category: 'inputs'
description: 'Capture user feedback limited to small set of options'
props: ['RadioGroup', 'Radio']
import: "import { RadioGroup, Radio } from '@mantine/core';"
source: 'mantine-core/src/components/RadioGroup/RadioGroup.tsx'
docs: 'core/RadioGroup.mdx'
styles: ['RadioGroup']
---

import { RadioGroupDemos } from '@demos/core';
import { RADIO_SIZES } from '@mantine/core';

## Usage

Use RadioGroup when you need to capture user feedback limited to certain options.
If you more that 5 options consider using [Select](/core/select/) instead of RadioGroup,
as it provides better UX for large data sets.

Component supports all props from [InputWrapper](/core/input-wrapper/) component.

<Demo data={RadioGroupDemos.configurator} />

## Size

Checkbox has 5 predefined sizes: xs, sm, md, lg, xl.
Size defines label font-size, input width and height.

<Demo data={RadioGroupDemos.sizes} />

You can get checkbox sizes by importing `RADIO_SIZES`:

```tsx
import { RADIO_SIZES } from '@mantine/core';
```

<DataTable
  head={['Prop value', 'Width and height']}
  data={Object.keys(RADIO_SIZES).map((size) => [size, `${RADIO_SIZES[size]}px`])}
/>

## Controlled

```tsx
import { useState } from 'react';
import { RadioGroup, Radio } from '@mantine/core';

function Demo() {
  const [value, setValue] = useState('react');

  return (
    <RadioGroup
      value={value}
      onChange={setValue}
      label="Select your favorite framework/library"
      description="This is anonymous"
      required
    >
      <Radio value="react">React</Radio>
      <Radio value="svelte">Svelte</Radio>
      <Radio value="ng">Angular</Radio>
      <Radio value="vue">Vue</Radio>
    </RadioGroup>
  );
}
```

## Get input ref

You can get input ref with `ref` prop on Radio component:

```tsx
import { useRef } from 'react';
import { Select } from '@mantine/core';

function Demo() {
  const ref = useRef();

  return (
    <RadioGroup>
      <Radio value="react">React</Radio>
      <Radio value="svelte" ref={ref}>
        Svelte
      </Radio>
    </RadioGroup>
  );
}
```
