---
group: 'mantine-core'
package: '@mantine/core'
title: TypographyStylesProvider
order: 1
slug: /core/typography-styles-provider/
category: 'typography'
description: 'Add Mantine typography styles to your html content'
props: ['TypographyStylesProvider']
import: "import { TypographyStylesProvider } from '@mantine/core';"
source: 'mantine-core/src/TypographyStylesProvider/TypographyStylesProvider.tsx'
docs: 'core/TypographyStylesProvider.mdx'
---

import { TypographyStylesProviderDemos } from '@mantine/demos';

## Usage

Mantine does not expose typography global styles.
Use `TypographyStylesProvider` to add Mantine typography styles to your html content:

```tsx
import { TypographyStylesProvider } from '@mantine/core';

function Demo() {
  return (
    <TypographyStylesProvider>
      <div dangerouslySetInnerHTML={{ __html: '<p>Your html here</p>' }} />
    </TypographyStylesProvider>
  );
}
```

## Example

<Demo data={TypographyStylesProviderDemos.usage} />

## All styles demo

`TypographyStylesProvider` includes styles for:

- paragraphs
- headings
- lists
- blockquotes
- tables
- links
- images
- hr
- code and pre

<Demo data={TypographyStylesProviderDemos.all} />
