---
package: mantine-core
title: Checkbox
order: 1
slug: /core/checkbox/
category: 'inputs'
---

import { CHECKBOX_SIZES } from '@mantine/core';
import { CheckboxIntermediateDemo } from '../demos/core/Checkbox/checkbox-intermediate.demo';
import { CheckboxConfigurator } from '../demos/core/Checkbox/checkbox.configurator';
import { CheckboxSizesDemo } from '../demos/core/Checkbox/checkbox-sizes.demo';

# Checkbox

<PageHeader
  source="mantine-core/src/Checkbox/Checkbox.tsx"
  docs="core/Checkbox.mdx"
  package="@mantine/core"
/>

```tsx
import { Checkbox } from '@mantine/core';
```

## Usage

Use Checkbox to capture boolean value input from user.
For better accessibility set `label` prop, it will add associated label element.

<CheckboxConfigurator />

## Sizes

Checkbox has 5 predefined sizes: xs, sm, md, lg, xl.
Size defines label font-size (from <GatsbyLink to="/pages/theming/">theme.fontSizes</GatsbyLink>), input width and height.

```tsx
<Checkbox size="xl" /> // -> predefined xl size
```

Checkbox sizes from xs to xl:

<CheckboxSizesDemo />

You can get checkbox sizes by importing `CHECKBOX_SIZES`:

```tsx
import { CHECKBOX_SIZES } from '@mantine/core';
```

<DataTable
  head={['Prop value', 'Width and height']}
  data={Object.keys(CHECKBOX_SIZES).map((size) => [size, `${CHECKBOX_SIZES[size]}px`])}
/>

## Intermediate state

Checkbox supports intermediate state. When `intermediate` prop is set to true,
`checked` prop is ignored:

<CheckboxIntermediateDemo />

## Controlled

```tsx
import React, { useState } from 'react';
import { Checkbox } from '@mantine/core';

function Demo() {
  const [checked, setChecked] = useState(false);
  return (
    <Checkbox checked={checked} onChange={(event) => setChecked(event.currentTarget.checked)} />
  );
}
```

## Get element ref

You can get input ref by setting `elementRef` prop:

```tsx
import React, { useRef } from 'react';
import { Checkbox } from '@mantine/core';

function Demo() {
  const ref = useRef();
  return <Checkbox elementRef={ref} />;
}
```

## Accessibility

Provide `aria-label` in case you use checkbox without label for screen reader support:

```tsx
<Checkbox label="My checkbox" />; // -> ok, input and label is connected
<Checkbox />; // not ok, input is not labeled
<Checkbox aria-label="My checkbox" />; // -> ok, label is not visible but will be announced by screen reader
```

## Component props

<PropsTable component="Checkbox" />
