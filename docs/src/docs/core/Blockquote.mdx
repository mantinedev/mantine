---
group: 'mantine-core'
package: '@mantine/core'
title: Blockquote
order: 1
slug: /core/blockquote/
category: 'typography'
description: 'Blockquote with optional cite'
props: ['Blockquote']
import: "import { Blockquote } from '@mantine/core';"
source: 'mantine-core/src/components/Blockquote/Blockquote.tsx'
docs: 'core/Blockquote.mdx'
styles: ['Blockquote']
---

import { BlockquoteDemos } from '@demos/core';

## Usage

<Demo data={BlockquoteDemos.configurator} />

## Configure icon

You can use any React node as icon by passing it to `icon` prop.
Note that component **does not control icon size** and, you will need to specify it manually on icon component for better fit.

<Demo data={BlockquoteDemos.icon} />

To remove icon, set `icon` prop to null:

<Demo data={BlockquoteDemos.noIcon} />

## TypeScript

@mantine/core package exports following types associated with Blockquote:

- `BlockquoteProps` – Blockquote component props, extends `React.ComponentProps<'blockquote'>`
- `BlockquoteStylesNames` – a union of styles names that can be used with [Styles API](/theming/styles-api/) (`classNames` and `styles` props)
