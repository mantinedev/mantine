---
package: mantine-core
title: Tooltip
order: 1
slug: /core/tooltip/
category: 'overlay'
description: 'Render tooltip on mouse over or click'
---

import { TooltipComplexDemo } from '../demos/core/Tooltip/tooltip-complex.demo';

# Tooltip

<PageHeader
  source="mantine-core/src/Tooltip/Tooltip.tsx"
  docs="core/Tooltip.mdx"
  package="@mantine/core"
/>

```tsx
import { Tooltip } from '@mantine/core';
```

## Usage

## Complex example

Use all above props to crete experience that fits your needs.
In this example tooltip acts more like popover – it is controlled,
can be closed with control inside tooltip (set `allowPointerEvents` prop to enable this),
has arrow and custom animation:

<TooltipComplexDemo />

## Component props

<PropsTable component="Tooltip" />
