---
package: mantine-core
title: 'Getting started'
order: 0
slug: /core/getting-started/
---

# Mantine core

@mantine/core is the main package that includes all essential components
that you may need to build an application.

## Installation

With yarn:

```sh
yarn add react-jss @mantine/hooks @mantine/core
```

With npm:

```sh
npm install react-jss @mantine/hooks @mantine/core
```

With [peerdeps](https://www.npmjs.com/package/install-peerdeps):

```sh
npx install-peerdeps @mantine/core
```

## Bundle size

<BundleSize packageName="@mantine/core" />

## Dependencies

<Dependencies
  packageName="@mantine/core"
  dependencies={[
    { name: '@mantine/hooks', type: 'peer' },
    { name: 'react', type: 'peer' },
    { name: 'react-dom', type: 'peer' },
    { name: 'react-jss', type: 'peer', why: 'CSS-in-JS library used to style all components' },
    {
      name: 'clsx',
      type: 'dependency',
      why: 'Utility to construct className stings conditionally',
    },
    {
      name: 'react-textarea-autosize',
      type: 'dependency',
      why: 'Utility component used to implement autosize in Textarea',
    },
    {
      name: 'react-transition-group',
      type: 'dependency',
      why: 'Used to implement transitions in Transition component',
    },
  ]}
/>

## License

<License />
