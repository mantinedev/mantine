---
package: mantine-core
title: Table
order: 1
slug: /core/table/
---

import { TableBaseDemo, TableFullDemo } from '../demos/core/Table/table.demos';
import { TableStrippedConfigurator } from '../demos/core/Table/table.configurator';

# Table

Render tables with single component

<PageHeader
  source="mantine-core/src/Table/Table.tsx"
  docs="core/Table.mdx"
  package="@mantine/core"
/>

```tsx
import { Table } from '@mantine/core';
```

Table data for all examples:

```tsx
const elements = [
  { position: 6, mass: 12.011, symbol: 'C', name: 'Carbon' },
  { position: 7, mass: 14.007, symbol: 'N', name: 'Nitrogen' },
  { position: 39, mass: 88.906, symbol: 'Y', name: 'Yttrium' },
  { position: 56, mass: 137.33, symbol: 'Ba', name: 'Barium' },
  { position: 58, mass: 140.12, symbol: 'Ce', name: 'Cerium' },
];
```

## General usage

Table component accepts the same children as table element and adds mantine theme styles.

<TableBaseDemo />

## Extra parts

Table support tfoot and caption elements.
Pass `captionSide` prop (top or bottom) to change caption position.

<TableFullDemo />

## Stripped and rows hover

You can make table striped by passing `stiped` prop to `Table`.
If `highlightOnHover` prop is true rows will be highlighted with gray color on hover.

<TableStrippedConfigurator />

## Component props

<PropsTable component="Table" />
