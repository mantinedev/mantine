---
group: 'mantine-core'
package: '@mantine/core'
title: TextInput
order: 1
slug: /core/text-input/
category: 'inputs'
description: 'Capture string input from user'
props: ['TextInput']
import: "import { TextInput } from '@mantine/core';"
source: 'mantine-core/src/components/TextInput/TextInput.tsx'
docs: 'core/TextInput.mdx'
styles: ['TextInput']
---

import { TextInputDemos } from '@demos/core';

## Usage

Use TextInput when you need to capture text from user.
Component supports all props from [Input](/core/input/) and [InputWrapper](/core/input-wrapper/) components:

<Demo data={TextInputDemos.configurator} />

## Controlled

```tsx
import { useState } from 'react';
import { TextInput } from '@mantine/core';

function Demo() {
  const [value, setValue] = useState('');
  return <TextInput value={value} onChange={(event) => setValue(event.currentTarget.value)} />;
}
```

## Invalid state and error

<Demo data={TextInputDemos.validation} />

## Disabled state

<Demo data={TextInputDemos.disabled} />

## With icon

You can use any React node as icon:

<Demo data={TextInputDemos.icon} />

## With right section

You can use any React node as right section:

<Demo data={TextInputDemos.rightSection} />

## Get element ref

You can get input ref with `elementRef` prop:

```tsx
import { useRef } from 'react';
import { TextInput } from '@mantine/core';

function Demo() {
  const ref = useRef(null);
  return <TextInput elementRef={ref} />;
}
```

## Server side rendering

Component uses [use-id](/hooks/use-id/) hook to generate unique ids and aria- attributes,
provide static `id` prop to prevent props mismatch:

```tsx
<TextInput /> // -> random id generated both on client and server, props mismatch warning
<TextInput id="my-input" /> // -> id is static, no mismatches
```

## Accessibility

Provide `aria-label` in case you use component without label for screen reader support:

```tsx
<TextInput label="My input" />; // -> ok, input and label is connected
<TextInput />; // not ok, input is not labeled
<TextInput aria-label="My input" />; // -> ok, label is not visible but will be announced by screen reader
```
