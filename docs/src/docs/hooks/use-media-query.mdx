---
package: mantine-hooks
title: 'use-media-query'
order: 1
slug: /hooks/use-media-query/
---

import { UseBreakpointDemo } from '../../demos/hooks/use-media-query.demos';
import PageHeader from '../../components/PageHeader/PageHeader';

# use-media-query

<PageHeader
  source="mantine-hooks/src/hooks/use-media-query/use-media-query.ts"
  docs="hooks/use-media-query.mdx"
  package="@mantine/hooks"
/>

use-media-query hook allows to subscribe to media queries. It receives media query as an argument and returns true if given media query matches current state. Hook relies on `window.matchMedia()` [API](https://developer.mozilla.org/en-US/docs/Web/API/Window/matchMedia) and will always return false if api is not available (e.g. during server side rendering).

## Usage

```tsx
import { useMediaQuery } from '@mantine/hooks';

function Example() {
  // matches will be true if (max-width: 765px) media query is satisfied
  const matches = useMediaQuery('(max-width: 765px)');
  return <div />;
}
```

<UseBreakpointDemo />
