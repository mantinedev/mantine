---
group: 'mantine-hooks'
package: '@mantine/hooks'
title: 'use-headroom'
category: 'utils'
order: 1
slug: /hooks/use-headroom/
description: 'Hide your header until you need it'
import: "import { useHeaderoom } from '@mantine/hooks';"
docs: 'hooks/use-headroom.mdx'
source: 'mantine-hooks/src/use-headroom/use-headroom.ts'
---

import { HooksDemos } from '@mantine/demos';

## Usage

use-headroom returns a boolean state which helps keep the header pinned, fixed or released (hidden)

<Demo data={HooksDemos.useHeadroomDemo} />

## Definition

```tsx
function useHeadroom({ fixAt?: number; onFix?(): void; onPin?(): void; onRelease?(): void; }): boolean;
```
