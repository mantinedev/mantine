---
package: mantine-hooks
title: 'use-id'
order: 1
slug: /hooks/use-id/
---

import PageHeader from '../../components/PageHeader/PageHeader';

# use-id

<PageHeader
  source="mantine-hooks/src/hooks/use-id/use-id.ts"
  docs="hooks/use-id.mdx"
  package="@mantine/hooks"
/>

use-id hook generates random id that persists across renders.
It is used to bind input elements to labels.

## Usage

```tsx
import { useId } from '@mantine/hooks';

const providedId = useId('email'); // returns given id -> email
const randomId = useId(); // generates random id if id was not provided –> mantine-j_YSXk1CdUip5gPldwCjy
```

## Example

```tsx
import React from 'react';
import { useId } from '@mantine/hooks';

function Example({ id }) {
  const uuid = useId(id);

  return (
    <>
      <label htmlFor={uuid}>Input label</label>
      <input type="text" id={uuid} />
    </>
  );
}
```
