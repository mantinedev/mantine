---
group: 'guides'
title: Usage with CRA
slug: /guides/cra/
search: 'Get started with create-react-app'
---

import { NextSteps } from '../../components/NextSteps/NextSteps.tsx';
import { PackagesInstallation } from '../../components/PackagesInstallation/PackagesInstallation.tsx';

# Usage with create-react-app

## Get started with template

The easiest way to get started is to use [the template on GitHub](https://github.com/mantinedev/mantine-cra-template):

1. Open [template page on GitHub](https://github.com/mantinedev/mantine-cra-template)
2. Click `Use this template` button
3. Clone or download the repository
4. Install dependencies: `yarn`
5. Start development server: `npm start`

## Generate new application

Init new application:

```bash
npx create-react-app my-mantine-app --template typescript
```

<PackagesInstallation />

Replace `App.tsx` with:

```tsx
import { MantineProvider, Text } from '@mantine/core';

export default function App() {
  return (
    <MantineProvider withGlobalStyles withNormalizeCSS>
      <Text>Welcome to Mantine!</Text>
    </MantineProvider>
  );
}
```

All set! Start development server:

```bash
npm start
```

## Next steps

<NextSteps />
