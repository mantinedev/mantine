---
group: 'Other packages'
package: '@mantine/nprogress'
title: 'Navigation progress'
order: 1
slug: /others/nprogress/
description: 'A navigation progress bar'
props: ['NProgress']
import: "import { NProgress } from '@mantine/nprogress';"
source: 'mantine-nprogress/src'
docs: 'others/nprogress.mdx'
installation: '@mantine/nprogress'
license: MIT
---

import { NProgressDemos } from '@mantine/demos';

## Installation

Package depends on @mantine/core and @mantine/hooks.

Install with npm:

```sh
npm install @mantine/nprogress @mantine/core @mantine/hooks
```

Install with yarn:

```sh
yarn add @mantine/nprogress @mantine/core @mantine/hooks
```

## Setup NProgress

Add `NProgress` to your app:

```tsx
import { MantineProvider } from '@mantine/core';
import { NProgress } from '@mantine/nprogress';

function App() {
  return (
    <MantineProvider>
      <NProgress />
      <YourApp />
    </MantineProvider>
  );
}
```

## Usage

<Demo data={NProgressDemos.usage} />

## Event based functions

`@mantine/nprogress` exports several functions which can be used to perform certain actions
from any part of your application:

```tsx
// All functions can be called from anywhere (not only from components)
import {
  addNProgress,
  decreaseNProgress,
  setNProgress,
  startNProgress,
  stopNProgress,
  resetNProgress,
} from '@mantine/nprogress';

// Adds progress to the current progress
addNProgress(10);

// Decreases the current progress
decreaseNProgress(10);

// Sets a custom progress
setNProgress(50);

// Starts the progress bar
startNProgress();

// Stops the progress bar
stopNProgress();

// Resets the progress bar
resetNProgress();
```
