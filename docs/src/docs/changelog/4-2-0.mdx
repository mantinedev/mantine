---
group: 'changelog'
title: 'Version 4.2.0'
order: 1
slug: /changelog/4-2-0/
release: https://github.com/mantinedev/mantine/releases/tag/4.2.0
date: 'April 27th, 2022'
---

import {
  ThemeDemos,
  IndicatorDemos,
  CalendarDemos,
  ModalDemos,
  HooksDemos,
  SegmentedControlDemos,
} from '@mantine/demos';

## primaryShade on theme

New `theme.primaryShade` property is used to determine which shade will be used for the components that have `color` prop,
it defaults to 6. Note that for some variants with dark color scheme `primaryShade` will not be used as it will cause contrast issues.

<Demo data={ThemeDemos.primaryShade} />

`primaryShade` can also be customized for dark and light color scheme separately:

```tsx
import { MantineProvider } from '@mantine/core';

function Demo() {
  return (
    <MantineProvider theme={{ primaryShade: { light: 6, dark: 8 } }}>
      <App />
    </MantineProvider>
  );
}
```

## Indicator component

New [Indicator](https://mantine.dev/core/indicator/) component:

<Demo data={IndicatorDemos.configurator} />

## use-focus-within hook

New [use-focus-within](https://mantine.dev/hooks/use-focus-within/) hook detects if any element within has focus,
it works the same way as `:focus-within` CSS selector:

<Demo data={HooksDemos.useFocusWithinDemo} />

## use-network hook

New [use-network](https://mantine.dev/hooks/use-network/) hook returns current connection status:

<Demo data={HooksDemos.useNetworkDemo} demoProps={{ toggle: true }} />

## New features

[Calendar](https://mantine.dev/dates/calendar/) component now supports multiple selected dates:

<Demo data={CalendarDemos.multiple} />

[Modal](https://mantine.dev/core/modal/) and [Drawer](https://mantine.dev/core/drawer/) components now support setting overlay blur:

<Demo data={ModalDemos.overlay} />

[Calendar](https://mantine.dev/dates/calendar/), [Month](https://mantine.dev/dates/month/) and [DatePicker](https://mantine.dev/dates/date-picker/) components
now support custom day renderer with `renderDay` function:

<Demo data={CalendarDemos.renderDay} />

[SegmentedControl](https://mantine.dev/core/segmented-control/) component now supports disabled items:

<Demo data={SegmentedControlDemos.disabled} />

## Other changes

- [use-window-event](https://mantine.dev/hooks/use-window-event/) hook now supports custom events
- [use-local-storage](https://mantine.dev/hooks/use-local-storage/) hook now supports value sync in the same tab
- [RichTextEditor](https://mantine.dev/others/rte/) now supports placeholder
