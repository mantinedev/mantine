---
group: 'changelog'
title: 'Version 5.9.0'
order: 1
slug: /changelog/5-9-0/
release: https://github.com/mantinedev/mantine/releases/tag/5.9.0
date: 'November 16th, 2022'
---

import { HooksDemos } from '@mantine/demos';

## use-eye-dropper hook

New [use-eye-dropper](https://mantine.dev/hooks/use-eye-dropper/) hook provides an interface to work with [EyeDropper API](https://developer.mozilla.org/en-US/docs/Web/API/EyeDropper_API).
It allows to pick color from any pixel on the screen. Currently, the API is supported in Chromium based desktop browsers.

<Demo data={HooksDemos.useEyeDropperUsage} />

## Static components default props

All static components now support [default props on MantineProvider](/theming/default-props/).
The following example demonstrates how to add default props to `Menu.Item`, `Tabs.List` and
`RichTextEditor.Toolbar` components:

```tsx
import { MantineProvider, Button } from '@mantine/core';

function Demo() {
  return (
    <MantineProvider
      theme={{
        components: {
          MenuItem: {
            defaultProps: { color: 'red' },
          },

          TabsList: {
            defaultProps: {
              position: 'right',
            },
          },

          RichTextEditorToolbar: {
            defaultProps: {
              sticky: true,
              stickyOffset: 60,
            },
          },
        },
      }}
    >
      <App />
    </MantineProvider>
  );
}
```

## Input.Placeholder component

`Input.Placeholder` component can be used to add placeholder to [Input and InputBase](https://mantine.dev/core/input/) components that are based on `button` element
or do not support placeholder property natively:

<Demo data={InputDemos.placeholder} />

## Other changes

- [RangeSlider](https://mantine.dev/core/slider/) component now supports `maxRange` prop
- [Stepper](https://mantine.dev/core/stepper/) component now supports any CSS color value in `color` prop
