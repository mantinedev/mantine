---
group: 'changelog'
title: 'Version 1.3.0'
order: 1
slug: /changelog/1-3-0/
release: https://github.com/mantinedev/mantine/releases/tag/1.3.0
date: 'June 1st, 2021'
---

import {
  CenterDemos,
  TabsDemos,
  RingProgressDemos,
  BadgeDemos,
  NumberInputDemos,
  HooksDemos,
} from '@mantine/demos';

## New components, hooks and features

New [RingProgress](https://mantine.dev/core/ring-progress/) component – display circular progress with multiple sections:

<Demo data={RingProgressDemos.usage} demoProps={{ toggle: true }} />

[Tabs](https://mantine.dev/core/tabs/#outline-variant) component now support outline variant:

<Demo data={TabsDemos.outline} demoProps={{ toggle: true }} />

[Badge](https://mantine.dev/core/badge/#colors) component now supports new dot variant:

<Demo data={BadgeDemos.dots} />

[NumberInput](https://mantine.dev/core/number-input/#custom-incrementdecrement-controls) component now exposes increment/decrement handlers via ref:

<Demo data={NumberInputDemos.handlers} demoProps={{ toggle: true }} />

New [Center](https://mantine.dev/core/center/) component – simple wrapper to center element horizontally and vertically:

<Demo data={CenterDemos.usage} demoProps={{ toggle: true }} />

New [use-interval](https://mantine.dev/hooks/use-interval/) hook provides simple wrapper around `window.setInterval`:

<Demo data={HooksDemos.useIntervalDemo} demoProps={{ toggle: true }} />

## Other changes

- [New documentation design](https://mantine.dev) with tabs and less cluttered packages index pages
- All packages are now published in 3 formats: esm (ECMAScript modules `export { Button } from './Button';`), cjs (commonjs `export.Button = Button;`) and umd (default for browser). These changes will allow webpack and similar bundlers to perform better tree shacking.
- All components now export related types: `import { ButtonProps } from '@mantine/core';`
- [use-focus-trap](https://mantine.dev/hooks/use-focus-trap/#initial-focus) hook now allows to change element that will initially receive focus

## Design changes

- [Button](https://mantine.dev/core/button/) component label is no longer uppercased by default, to replicate old design use `<Button uppercase />`
- Colors saturation and text shadows adjusted for [Button](https://mantine.dev/core/button/) and [Badge](https://mantine.dev/core/badge/) components for better contrast
