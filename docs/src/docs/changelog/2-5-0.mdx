---
group: 'changelog'
title: 'Version 2.5.0'
order: 1
slug: /changelog/2-5-0/
release: https://github.com/mantinedev/mantine/releases/tag/2.5.0
date: 'September 16th, 2021'
---

import {
  TimelineDemos,
  DialogDemos,
  ListDemos,
  ChipsDemos,
  PaginationDemos,
  TimeRangeInputDemos,
  RichTextEditorDemos,
} from '@mantine/demos';

## UMD builds deprecation

All `@mantine/` packages will no longer default to umd builds via `browser` field in `package.json`.
This change will simplify tree shacking for Webpack and should not affect your Next.js, Gatsby, Vite or CRA applications.

## react-jss peer dependency removal

All `@mantine/` packages no longer require react-jss installation as peer dependency – it will be installed automatically as @mantine/core dependency.
Instead of direct react-jss usage @mantine/core now exports [createStyles](https://mantine.dev/theming/react-jss/) function
and a [set of components](https://mantine.dev/theming/next/) to implement server side rendering.

### createStyles function

New `createStyles` function lets you create styles with Mantine theme:

```tsx
import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
  button: {
    backgroundColor: theme.colors.blue[6],
    color: theme.white,
    padding: `${theme.spacing.md}px ${theme.spacing.lg}px`,
  },
}));

function Demo() {
  const classes = useStyles();
  return <button className={classes.button}>My button</button>;
}
```

Note that it will only work if your application is wrapped with MantineProvider.

### Builtin server side rendering

@mantine/core package now comes with components to simplify server side rendering setup with Next.js and any other environment.
Checkout [Next.js guide](https://mantine.dev/theming/next/) to learn more.

## New package @mantine/rte

[@mantine/rte](https://mantine.dev/others/rte/) is a new package with RichTextEditor component.
Component integrates seamlessly with your MantineTheme and provides all basic rich text editing features:

<Demo data={RichTextEditorDemos.usage} />

## New components

[Timeline](https://mantine.dev/core/timeline/) component lets you show list of events in chronological order:

<Demo data={TimelineDemos.usage} demoProps={{ toggle: true }} />

Navigate between multiple pages with new [Pagination](https://mantine.dev/core/pagination/) component:

<Demo data={PaginationDemos.configurator} />

[Chips](https://mantine.dev/core/chips/) component is an inline alternative to [RadioGroup](https://mantine.dev/core/radio-group/) and [MultiSelect](https://mantine.dev/core/multi-select/) components:

<Demo data={ChipsDemos.configurator} />

With [Dialog](https://mantine.dev/core/dialog/) component you can display fixed overlay at any side of the screen:

<Demo data={DialogDemos.usage} demoProps={{ toggle: true }} />

[List](https://mantine.dev/core/list/) component is a wrapper for ul and ol lists with option to replace bullets with icons:

<Demo data={ListDemos.icon} demoProps={{ toggle: true }} />

[TimeRangeInput](https://mantine.dev/dates/time-range-input/) component allows to capture time range from user:

<Demo data={TimeRangeInputDemos.usage} demoProps={{ toggle: true }} />

## New hooks

- [use-pagination](https://mantine.dev/hooks/use-pagination/) – manage pagination state
- [use-focus-return](https://mantine.dev/hooks/use-focus-return/) – capture last focused element on the page and return focus to it once condition is met

## New features

- [Button](https://mantine.dev/core/button/#white-variant) component now supports new `white` variant
- [Text](https://mantine.dev/core/text/#inherit-styles) now supports font styles inheritance from parent element with `inherit` prop
- [Paper](https://mantine.dev/core/paper/) component now supports `withBorder` prop to add 1px border
- [ActionIcon](https://mantine.dev/core/action-icon/) component now supports `loading` state (same as in [Button](https://mantine.dev/core/button/) component)
- [SegmentedControl](https://mantine.dev/core/segmented-control/) component now supports setting data as an array of strings `<SegmentedControl data={['React', 'Angular', 'Svelte', 'Vue']} />`
- [NumberInput](https://mantine.dev/core/number-input/#decimal-steps) component now supports decimal steps and precision
